var request, API_ADDRESS;

request = require('superagent');
API_ADDRESS = 'http://www.hel.fi/palvelukarttaws/rest/v2';

module.exports.getOrganizations = function (cb) {
  request.get(API_ADDRESS + '/organization/').end(function (res) {
    return cb(res.body);
  })
}